namespace ProjectOfNguyenTrungKien.Enum
{
    public enum ErrorCodeNotFound
    {
        NOT_FOUND_PRODUCT = 1000,
        NOT_FOUND_CATEGORY = 1001,
    }

    public enum ErrorCodeBadRequest
    {
        BAD_REQUEST_PRODUCT = 2000,
        BAD_REQUEST_CATEGORY = 2001,
    }

    public enum ErrorCodeExists
    {
        EXISTS_PRODUCT = 3000,
        EXISTS_CATEGORY = 3001,
    }
}
